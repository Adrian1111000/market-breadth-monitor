# Copy to .env and fill in. .env is gitignored and must never be committed.
#
# Free key from https://polygon.io/dashboard/api-keys
POLYGON_API_KEY=your_key_here

# Optional. Free tier is 5 requests/minute; set to 0 for paid plans.
# MR_RATE_LIMIT_PER_MIN=5
